<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="team.tb.dao.ClazzMapper">
  <resultMap id="BaseResultMap" type="team.tb.pojo.Clazz">
    <id column="cid" jdbcType="INTEGER" property="cid" />
    <result column="cnum" jdbcType="VARCHAR" property="cnum" />
    <result column="cmajor" jdbcType="VARCHAR" property="cmajor" />
  </resultMap>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from t_class
    where cid = #{cid,jdbcType=INTEGER}
  </delete>
  <insert id="insert" parameterType="team.tb.pojo.Clazz">
    insert into t_class (cid, cnum, cmajor
      )
    values (#{cid,jdbcType=INTEGER}, #{cnum,jdbcType=VARCHAR}, #{cmajor,jdbcType=VARCHAR}
      )
  </insert>
  <update id="updateByPrimaryKey" parameterType="team.tb.pojo.Clazz">
    update t_class
    set cnum = #{cnum,jdbcType=VARCHAR},
      cmajor = #{cmajor,jdbcType=VARCHAR}
    where cid = #{cid,jdbcType=INTEGER}
  </update>
  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select cid, cnum, cmajor
    from t_class
    where cid = #{cid,jdbcType=INTEGER}
  </select>
  <select id="selectAll" resultMap="BaseResultMap">
    select cid, cnum, cmajor
    from t_class
  </select>
<!--  根据院系id获取班级，保证班号顺序-->
  <select id="getClazzByDepartmentId" resultMap="BaseResultMap">
    select cid, cnum, cmajor
    from t_class
    where cmajor = #{majorId} order by cnum
  </select>
</mapper>
